
# Logging accepts CRITICAL, FATAL, ERROR, WARNING, WARN, INFO, DEBUG, NOTSET.
LOG_LEVEL=INFO

# Set to `true` to extract tables as clean JSON blocks instead of standard Markdown grid tables.
MARKITDOWN_EXTRACT_TABLES_AS_JSON=false
