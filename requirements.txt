# Legacy pip-based installation support.
# Preferred workflow: `uv sync --extra dev` using pyproject.toml.
fastapi>=0.128.8
uvicorn[standard]>=0.40.0
markitdown[all]>=0.1.5
python-multipart>=0.0.20
pytest>=9.0.2
httpx>=0.28.1
